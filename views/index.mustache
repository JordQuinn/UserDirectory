<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Document</title>
  <link rel="stylesheet" href="css/main.css" />
</head>
<body>
  <h3> Refine your search:</h3>

<form action = "/looking/add" method="post">
  <button type="text" name="looking">Looking for work</button>
</form>

<form action = "/employed/add" method="post">
  <button type="text" name = "employed">Employed</button>
</form>

  <ul id = "users">
    {{#users}}
    <li>
    <a href="/user/{{username}}">
      <img src="{{avatar}}" />
      <span class = "name">{{name}}</span>
      <span class = "position"> {{job}}</span>
    {{#company}}
      <span class = "company"> {{company}}</span>
    {{/company}}
      {{^company}}<span class = "null" style= color:red>Available for Hire</span>
      {{/company}}
    </a>
    </li>
    {{/users}}
  </ul>
</body>
</html>
